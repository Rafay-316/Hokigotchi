# Copy these three names into your existing web/.env.local.
# Keep the NESSIE settings already in that file. Never paste real keys into chat.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server secret (sb_secret_...) or legacy service_role key. NEVER prefix it NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
